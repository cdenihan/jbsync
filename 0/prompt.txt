So I small thing in your config. Why do you have to define the setup like that so it doesn't install in IDEAs except CLion. Can you make it easier for a user to do it by only configuring one rule? Also, I don't know if examples exist for this, but if they don't please give examples in the documentation on config things such as this. There should also be a CLI command that can be run to set all this in the config file. Don't make a PR for these changes, just commit to main