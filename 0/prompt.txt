The sync engine is missing settings. I changed the setting to allow the TypeScript language server to use more RAM than it was originally allowed. I told it that it can dynamically grab as much as it needs. I set the setting in WebStorm, yet the sync engine isn’t picking it up. Please look into this and figure out why, and make a PR fixing the bug.

Wait, actually, don’t make a PR—just test your changes locally and then push them to main. When you push, first make the repository public, push it, let all the CI finish, and then make it private again.